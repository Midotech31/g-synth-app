# Lint configuration lives at the repository root, in `ruff.toml`, so the
# engine and the HTTP layer are held to the same rules. A second copy here
# meant `ruff check` from the root quietly used its defaults instead — and
# an undefined name sat unreported in `verify.py`, invisible at runtime too
# because `from __future__ import annotations` never evaluates annotations.
